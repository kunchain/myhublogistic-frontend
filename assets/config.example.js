// Example configuration for Myhublogistic frontend
// Copy this file to config.js and adjust values, or override directly in app.js.

// Base URL for the backend API (no trailing slash)
const API_BASE_URL = "https://api.myhublogistic.com";

// Optional flags
const CONFIG = {
  // Enable debug logging in app.js
  debug: false,

  // Default redirect paths after login
  adminRedirect: "admin-users.html",
  clientRedirect: "client-dashboard.html"
};
